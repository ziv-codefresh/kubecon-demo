should_fail_step:
  title: test
  description: should faild because it will require more the the allowed memory
  image: image/id
  commands:
  - curl -o mygettext.html http://www.gnu.org/software/gettext/manual/gettext.html
